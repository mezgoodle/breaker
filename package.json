{
  "name": "breaker",
  "version": "0.1.0",
  "description": "Application on Electron",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "lint:fix": "eslint ./ --fix",
    "test": "eslint ./",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=assets/icons/mac/icon.icns --prune=true --out=release-builds",
    "package-win": "electron-packager . electron-tutorial-app --overwrite --asar --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Breaker he\"",    
    "package-linux": "electron-packager . electron-tutorial-app --overwrite --asar=true --platform=linux --arch=x64 --icon=assets/icons/png/icon.png --prune=true --out=release-builds"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mezgoodle/breaker.git"
  },
  "keywords": [
    "nodejs",
    "electron",
    "timer"
  ],
  "author": "Maxim Zavalniuk",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/mezgoodle/breaker/issues"
  },
  "homepage": "https://github.com/mezgoodle/breaker#readme",
  "devDependencies": {
    "electron": "^9.1.1",
    "electron-packager": "^15.0.0",
    "eslint": "^7.5.0"
  },
  "dependencies": {
    "electron": "^9.1.1"
  }
}
